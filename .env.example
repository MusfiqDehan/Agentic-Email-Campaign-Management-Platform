# Root Level Environment Configuration Example
# Copy this file to .env.local and customize for your monorepo environment

# Environment
ENVIRONMENT=local

# Database Configuration (shared between services)
DB_NAME=email_campaign_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=25432

# Redis Configuration (shared)
REDIS_HOST=localhost
REDIS_PORT=26379

# Application URLs
BACKEND_URL=http://localhost:28000
FRONTEND_URL=http://localhost:3000

# Backend specific environment variables are in backend/.env.example
# Frontend specific environment variables will be in frontend/.env.example
